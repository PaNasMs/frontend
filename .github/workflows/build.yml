name: Build PaNasMs
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: build-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  build:
    uses: PaNasMs/panasms/.github/workflows/build.yml@12aaf283d43904f5c551c1d1a040e33e82cfb4ae
    with:
      build_ref: 12aaf283d43904f5c551c1d1a040e33e82cfb4ae
      frontend_ref: ${{ github.sha }}
